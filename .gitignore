node_modules/
coverage/
dist/
.turbo/
supabase/.temp/
.env
.env.local
.env.*.local
*.tsbuildinfo

# SBOM e binário de secret scanning baixados localmente (ci:local/CI) — nunca versionar
sbom.json
gitleaks

# Marcador de story ativa (local por sessão, não vai para o repo)
.current-story

# Cache gerado do graphify (grafo de conhecimento) — regenerável via `graphify update .`
graphify-out/
apps/web/graphify-out/

# Playwright (e2e) — sessão de auth salva (token real) e artefatos de execução, nunca versionar
apps/web/e2e/.auth/
apps/web/playwright-report/
apps/web/test-results/
apps/web/blob-report/
