// @sinergica/database — tipos gerados do Postgres (Supabase).
// Os tipos reais são gerados por `pnpm --filter @sinergica/database gen` (ver script "gen")
// e ficam em src/types.gen.ts. Não editar à mão.
// Placeholder inicial — preenchido quando o schema dos contextos estiver materializado.

export const DATABASE_PACKAGE = "@sinergica/database";
