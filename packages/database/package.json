{
  "name": "@sinergica/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tipos gerados do Postgres (supabase gen types typescript). Não editar à mão.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "gen": "echo 'supabase gen types typescript --project-id <ref> > src/types.gen.ts'"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
