// App shell placeholder do Sinérgica OS.
// A casca: o roteamento por bounded context (features/*) entra na fase de construção.
// Ver docs/ARCHITECTURE.md (context-map) e apps/web/README.md.

export function App() {
  return (
    <main>
      <h1>Sinérgica OS</h1>
      <p>Sistema operacional da Sinérgica Manutenções — casca inicial (Padrão OS v2).</p>
    </main>
  );
}
